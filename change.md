# Как обновить сайт студии ТОНУС

Все тексты, которые могут меняться — цена, телефон, часы, отзывы, вопросы — хранятся в одном файле: **content.js**. Менять нужно только его. `index.html` трогать не нужно.

---

## Часть 1. Как открыть файл для редактирования

### Шаг 1. Войдите в Beget

Откройте браузер и зайдите на **beget.com** → кнопка «Войти» → введите логин и пароль от хостинга.

### Шаг 2. Откройте файловый менеджер

В панели управления найдите раздел **«Файловый менеджер»** и нажмите на него.

### Шаг 3. Найдите папку сайта

Вы увидите список папок. Откройте папку **`tonus-studio.ru`** → затем папку **`public_html`**.

Вы увидите файлы сайта, среди них:
- `index.html` — сам сайт (не трогать)
- **`content.js`** — файл с текстами (редактировать здесь)

### Шаг 4. Откройте content.js для редактирования

Нажмите правой кнопкой мыши на файл **`content.js`** → выберите **«Редактировать»** (или «Edit»).

Откроется текстовый редактор прямо в браузере.

---

## Часть 2. Что и как менять

> **Важное правило:** не удаляйте кавычки `'`, запятые `,` и скобки. Меняйте только текст внутри кавычек.

---

### Изменить номер телефона

Найдите строку:
```
phone: '+79164940333',
```
Замените номер внутри кавычек (без пробелов и скобок):
```
phone: '+79161234567',
```

Следующую строку тоже обновите — это номер для отображения на сайте:
```
phoneDisplay: '+7 (916) 123-45-67',
```

> Меняйте обе строки сразу — иначе ссылка и текст не совпадут.

---

### Изменить часы работы

Найдите три строки:
```
hoursFull:  '8:00 — 18:00, Вс — выходной',
hoursShort: '8:00 — 18:00, Вс — вых.',
```

Измените оба варианта (они используются в разных местах сайта):
```
hoursFull:  '9:00 — 20:00, Вс — выходной',
hoursShort: '9:00 — 20:00, Вс — вых.',
```

---

### Изменить цену в FAQ

Найдите первый вопрос в разделе `faq:`:
```
q: 'Сколько стоит занятие?',
a: '<p>...В среднем это <strong>4 000 рублей</strong>...</p>'
```

Замените `4 000 рублей` на новую цену, например `4 500 рублей`. Остальной текст не трогайте.

---

### Добавить новый отзыв

Найдите раздел `reviews:` — он выглядит так:
```
reviews: [
  {
    name:    'Надежда',
    initial: 'Н',
    text:    '«Текст отзыва»',
    meta:    '30 мая 2024 · Лимфодренажный массаж'
  },
  ...
]
```

Чтобы добавить новый отзыв — **вставьте новый блок в самое начало**, перед «Надеждой»:

```
reviews: [
  {
    name:    'Ирина',
    initial: 'И',
    text:    '«Текст нового отзыва»',
    meta:    '15 января 2026 · Остеопракт'
  },
  {
    name:    'Надежда',
    ...
```

> **Правило:** после закрывающей фигурной скобки `}` каждого отзыва (кроме последнего) должна стоять запятая `,`. У последнего отзыва запятая не нужна.

Поле `initial` — первая буква имени, отображается в кружке-аватаре.

---

### Изменить текст существующего отзыва

Найдите нужный отзыв по имени и замените текст в поле `text:`. Кавычки `«»` внутри текста оставьте — они часть стиля.

---

### Удалить отзыв

Найдите блок отзыва — от `{` до `}` включительно — и удалите его вместе с запятой после него.

**Пример:** было три отзыва, хотим оставить два:
```
// Было:
  { name: 'Надежда', ... },   ← удалить эту строку и всё между { }
  { name: 'Олег', ... },
  { name: 'Александр', ... }

// Стало:
  { name: 'Олег', ... },
  { name: 'Александр', ... }
```

---

### Изменить вопрос/ответ в FAQ

Найдите раздел `faq:`. Каждый вопрос выглядит так:
```
{
  q: 'Сколько стоит занятие?',
  a: '<p>Текст ответа здесь.</p>'
},
```

Поле `q` — это вопрос, `a` — ответ. Меняйте текст внутри кавычек.

> В ответе могут встречаться теги вроде `<strong>жирный текст</strong>` или `<p>абзац</p>`. Их не удаляйте — они отвечают за форматирование.

---

### Изменить кейс (историю клиента)

Найдите раздел `cases:`. Каждый кейс содержит три поля:
```
{
  situation: 'Описание ситуации клиента.',
  action:    'Что делали во время сеансов.',
  result:    'Итог и результат.'
},
```

Просто замените текст в любом поле.

---

## Часть 3. Сохранение и проверка

### Шаг 1. Сохраните файл

После правок нажмите **«Сохранить»** (или кнопку Save) в редакторе Beget.

### Шаг 2. Откройте сайт

Перейдите на **tonus-studio.ru** в браузере.

Если изменения не видны — нажмите **Ctrl + Shift + R** (на Windows) или **Cmd + Shift + R** (на Mac). Это жёсткое обновление страницы — сбрасывает кэш браузера.

### Шаг 3. Убедитесь, что всё выглядит правильно

Проверьте раздел, в котором вносили изменения — текст должен обновиться.

---

## Часть 4. Если что-то пошло не так

**Сайт не открывается или выглядит сломанным** — вероятно, случайно удалили кавычку, запятую или скобку.

Что делать:
1. Откройте `content.js` снова
2. Найдите место, которое редактировали
3. Проверьте, что все `'`, `,`, `{`, `}` на месте
4. Сохраните и обновите страницу

**Не можете найти нужное место** — воспользуйтесь поиском в редакторе: нажмите **Ctrl + F** и введите слово, которое ищете (например, «Надежда» или «стоит занятие»).

**Не уверены в правках** — напишите Дмитрию, он поможет.

---

## Шпаргалка: что где находится

| Что менять | Где искать в файле |
|---|---|
| Телефон | `phone:` и `phoneDisplay:` |
| Telegram | `telegram:` |
| Email | `email:` |
| Часы работы | `hoursFull:` и `hoursShort:` |
| Цена (в FAQ) | первый вопрос в `faq:` |
| Отзывы | раздел `reviews:` |
| Истории клиентов | раздел `cases:` |
| Вопросы и ответы | раздел `faq:` |
